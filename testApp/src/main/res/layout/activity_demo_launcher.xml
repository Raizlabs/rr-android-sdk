<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/margin.standard"
    android:gravity="center"
    android:orientation="vertical">

    <EditText
        android:id="@+id/clientKeyEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:imeOptions="actionNext"
        android:singleLine="true"
        android:hint="@string/launcher.clientKeyHint"/>

    <EditText
        android:id="@+id/clientNameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:imeOptions="actionDone"
        android:singleLine="true"
        android:hint="@string/launcher.clientNameHint"/>

    <Button
        android:id="@+id/preferencesDemoButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/launcher.preferences_button" />

    <Button
        android:id="@+id/findDemoButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/launcher.find_button" />

</LinearLayout>